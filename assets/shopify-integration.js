// Shopify Integration helpers
console.log('Shopify integration loaded');

// Helper to add watermarked image to cart
window.addWatermarkedImageToCart = function(options = {}) {
  console.log('Adding watermarked image to cart:', options);
  // This is a placeholder for future cart integration
  return true;
}; 